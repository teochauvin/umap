from setuptools import find_packages, setup

setup(
    name='umap', 
    packages=find_packages(include=['umap']),
    version='0.1.0', 
    description='Umap is a library designed to get, extract, analyze and build realistic urban environment and flows.', 
    author='Téo Chauvin', 
    author_email='teo.chauvin@enac.fr', 
)

